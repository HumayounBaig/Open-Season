import { styled } from "styled-components"

export const DashboardContainer = styled.div`
  margin-left: auto;
  margin-right: auto;
  max-width: 100%;
  align-items: center;
  padding: 24px;

  background: linear-gradient(45deg, #047857, #1c64d8);
  height: fit-content;
  box-shadow: 0 0 0.75rem rgba(0, 0, 0, 0.2);
`
